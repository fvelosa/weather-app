/**
 * @ngdoc directive
 * @name weatherIcon
 * @restrict A
 * @description
 *
 */
angular.module('app')

    .directive('weatherIcon', function () {
        return {
            restrict: 'A',
            link: function link(scope, element, attrs) {

            }
        };
    });